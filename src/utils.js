/**More Complex Styled app version  6 - 'utils js' - Features:
 * (Alternative approach to build Global Variables)
 * 
 *        --> Building a 'Global Variable' to keep and distribute
 *            a color.
 * 
 * Note: this second approach dies the same that targeting ':root'
 * element, this time i'll use this 'utils' file to keep the 
 * styles
 */

/**this will be the Style component that will keep the 
 * 'Global Variable' -this case 'colors'-
 */
export const colors = {
    primary: '#e5d0ff'
}

