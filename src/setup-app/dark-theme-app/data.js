/**DarkTheme app version  1 - 'data' js file - 
 * Features:
 * 
 *        --> Setting up data to use on 'DarkTheme' app.
 * 
 * Note: this app is about implementing Theming from 
 * 'styled-components' library, speifically 'ThemeProvider' 
 * Component in order to build a 'DarkTheme' app
 *
 * 'BaseTheme' ( Lighter ) is the oposite values
 *  to 'DarkTheme' ( Darker )
 */
export const BaseTheme = {
    color: '#222',
    background: '#fff'
  }
  
export const DarkTheme = {
    color: '#fff',
    background: '#222'
  }